import React from "react";
import "./ListPromotion";

const ListPromotion = () => {
  /*  const [promotions, setpromotions] = useState([])
     const [loading, setloading] = useState(false)
     const history = useHistory();
     const [HanoutSerch, setHanoutSerch] = useState(null)
     const [hanouts, sethanouts] = useState([])
     const [cookies, setCookie] = useCookies(['userId']);
  */
  return (
    <div>
      {/*  <div className="tableCategorie">
                <TableData dataBody={produits} tableHead={tableHead} loading={loading} delete={handleClick} typeData="promotion" getCat={getCategorie} />
            </div>
 */}
    </div>
  );
};

export default ListPromotion;
